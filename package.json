{
  "name": "soundcloud-dl-server",
  "version": "1.0.0",
  "description": "SoundCloud MP3 download proxy server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "pip install yt-dlp || true"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "node-fetch": "^2.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
